/**
 * This function checks if the string passed in is a palindrome.
 * @param {String}
 * @return {boolean}
 */

var palidrome = function(str){
  //  Automatically return false if the parameter sent is not a string.
  if(typeof(str) !== 'string'){
    return false;
  }
  else{

  }
};
